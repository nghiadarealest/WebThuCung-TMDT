@model Inventory

<div class="modal fade bd-example-modal-lg" id="inventory-add" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Thêm mới</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form asp-action="Add" method="post" id="inventory--add">
                    <div class="row">
                        <div class="col">
                            <div class="form-group">
                                <label>Mã sản phẩm</label>
                                <select class="form-control" name="ProductId" asp-for="ProductId" required>
                                    <option value="">Chọn sản phẩm</option>
                                    @foreach (var product in ViewBag.Products)
                                    {
                                        <option value="@product.ProductId">@product.ProductName</option>
                                    }
                                </select>
                            </div>
                        </div>
                        <div class="col">
                            <div class="form-group">
                                <label>Số lượng trong kho</label>
                                <input type="number" class="form-control" placeholder="Enter quantity in stock" name="QuantityInStock" asp-for="QuantityInStock" min="0" required title="Số lượng không được âm">
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col">
                            <div class="form-group">
                                <label>Cập nhật cuối</table></table></label>
                                <input type="datetime-local" class="form-control" name="LastUpdated" asp-for="LastUpdated" required>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-dismiss="modal" style="border-radius: 20px;">Đóng</button>
                <button type="submit" class="btn btn-outline-primary" style="border-radius: 20px;" form="inventory--add">Lưu</button>
            </div>
        </div>
    </div>
</div>